name: "weather_dbt"
version: "1.0.0"
config-version: 2

profile: "weather_dbt"

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  weather_dbt:
    staging:
      +schema: staging_models
      +materialized: view
    marts:
      +schema: core
      +materialized: table
    bi:
      +schema: bi
      +materialized: view